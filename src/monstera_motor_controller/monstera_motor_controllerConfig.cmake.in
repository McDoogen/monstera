# - Config file for the monstera_motor_controller package
# It defines the following variables
#  monstera_motor_controller_INCLUDE_DIRS - include directories for FooBar
#  monstera_motor_controller_LIBRARIES    - libraries to link against
#  monstera_motor_controller_EXECUTABLE   - the bar executable

set(monstera_motor_controller_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${monstera_motor_controller_DIR}/export_monstera_motor_controller.cmake")

# These are IMPORTED targets created by monstera_motor_controllerTargets.cmake


